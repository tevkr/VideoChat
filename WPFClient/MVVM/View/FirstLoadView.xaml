<UserControl x:Class="WPFClient.MVVM.View.FirstLoadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFClient.MVVM.View"
             xmlns:viewModel="clr-namespace:WPFClient.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:FirstLoadViewModel/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Введите имя пользователя: "/>
                <TextBox x:Name="UserNameTextBox" Width="150" Text="{Binding userNameTextBoxText, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <Button x:Name="EnterButton" Content="Войти" Command="{Binding enterCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
